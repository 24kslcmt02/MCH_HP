name: Deploy to Sakura Server

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v4

      # Node.jsのセットアップ（これがないとnpmコマンドが使えません）
      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      # ビルド実行
      - name: 🔨 Build Project
        run: |
          npm ci
          npm run build

      # FTPアップロード (LFTPを使用)
      - name: 📂 Sync files via LFTP
        run: |
          sudo apt-get update
          sudo apt-get install -y lftp
          lftp -f "
          open ${{ secrets.FTP_SERVER }}
          user ${{ secrets.FTP_USERNAME }} ${{ secrets.FTP_PASSWORD }}
          set ssl:verify-certificate no
          set ftp:ssl-allow no
          mirror -R -v --parallel=4 --exclude .git/ --exclude .github/ --exclude node_modules/ ./dist/ ./www/ms-capital/
          bye
          "
